name: Atualização diária

on:
  schedule:
    - cron: '0 11 * * *'
  workflow_dispatch:

jobs:
  atualizar:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do repositório
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependências
        run: pip install nba_api requests python-dotenv

      - name: Rodar scripts de coleta
        env:
          BDL_API_KEY: ${{ secrets.BDL_API_KEY }}
        run: |
          python3 coletar_playoffs.py
          python3 coletar_head2head.py
          python3 coletar_todos_jogos.py
          python3 corrigir_starter.py

      - name: Atualizar salários
        run: python3 generate_salaries.py
        env:
          BDL_API_KEY: ${{ secrets.BDL_API_KEY }}

      - name: Commit e push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Atualização automática $(date '+%Y-%m-%d')"
          git push
